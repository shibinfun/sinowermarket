<div class="min-h-[calc(100vh-12rem)] flex items-center justify-center py-16 px-4 sm:px-6 lg:px-8 bg-slate-50/50">
  <div class="max-w-md w-full">
    <div class="bg-white rounded-3xl shadow-xl shadow-slate-200/60 p-8 sm:p-10 border border-slate-100">
      <div class="mb-10 text-center">
        <%= image_tag "logo.png", alt: "Sinower Market", class: "mx-auto h-14 w-auto" %>
        <h2 class="mt-6 text-3xl font-bold tracking-tight text-slate-900"><%= t('.title') %></h2>
        <p class="mt-2 text-sm text-slate-500"><%= t('.subtitle') %></p>
      </div>
      
      <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "space-y-6" }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div>
          <%= f.label :email, t('.email'), class: "block text-sm font-semibold text-slate-700 mb-1.5 ml-1" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "block w-full px-4 py-3.5 bg-slate-50 border-0 text-slate-900 rounded-2xl ring-1 ring-inset ring-slate-200 placeholder:text-slate-400 focus:ring-2 focus:ring-inset focus:ring-blue-600 focus:bg-white sm:text-sm sm:leading-6 transition-all duration-200", placeholder: t('.email') %>
        </div>

        <div>
          <%= f.submit t('.submit'), class: "w-full flex justify-center py-4 px-4 border border-transparent text-sm font-bold rounded-2xl text-white bg-blue-600 hover:bg-blue-700 shadow-lg shadow-blue-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-600 cursor-pointer transition-all duration-200 active:scale-[0.98]" %>
        </div>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
