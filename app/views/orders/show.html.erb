<div class="max-w-4xl mx-auto px-4 py-8">
  <div class="flex justify-between items-center mb-8">
    <h1 class="text-3xl font-bold"><%= t('.order_number', id: @order.id) %></h1>
    <span class="px-4 py-2 rounded-full font-semibold <%= 
      case @order.status
      when 'paid' then 'bg-green-100 text-green-800'
      when 'cancelled' then 'bg-red-100 text-red-800'
      else 'bg-yellow-100 text-yellow-800'
      end %>">
      <%= @order.status.capitalize %>
    </span>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
    <div class="bg-white shadow rounded-lg p-6">
      <h2 class="text-xl font-semibold mb-4 border-b pb-2"><%= t('.shipping_details') %></h2>
      <p><strong><%= t('.name') %></strong> <%= @order.name %></p>
      <p><strong><%= t('.phone') %></strong> <%= @order.phone %></p>
      <p><strong><%= t('.address') %></strong> <%= @order.province %> <%= @order.city %> <%= @order.district %></p>
      <p class="ml-16"><%= @order.detail_address %></p>
    </div>

    <div class="bg-white shadow rounded-lg p-6">
      <h2 class="text-xl font-semibold mb-4 border-b pb-2"><%= t('.order_info') %></h2>
      <p><strong><%= t('.date') %></strong> <%= l(@order.created_at, format: :long) rescue @order.created_at.strftime("%B %d, %Y %H:%M") %></p>
      <p><strong><%= t('.total_amount') %></strong> <span class="text-xl font-bold text-blue-600"><%= format_price(@order.total_price, @order.currency) %></span></p>
      <p><strong><%= t('.currency') %></strong> <span class="text-sm font-semibold text-slate-500"><%= @order.currency %></span></p>
      
      <% if @order.status == 'pending' %>
        <div class="mt-4 space-y-2">
          <%= button_to t('.pay_now'), pay_order_path(@order), method: :post, class: "w-full bg-green-600 text-white px-6 py-2 rounded-lg font-bold hover:bg-green-700 transition" %>
          <%= button_to t('.cancel_order'), cancel_order_path(@order), method: :post, data: { confirm: t('.are_you_sure') }, class: "w-full bg-gray-100 text-gray-700 px-6 py-2 rounded-lg font-semibold hover:bg-gray-200 transition" %>
        </div>
      <% end %>
    </div>
  </div>

  <div class="bg-white shadow rounded-lg overflow-hidden">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50">
        <tr>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase"><%= t('.product') %></th>
          <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase"><%= t('.price') %></th>
          <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase"><%= t('.quantity') %></th>
          <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase"><%= t('.subtotal') %></th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        <% @order.order_items.each do |item| %>
          <tr>
            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900"><%= item.name %></td>
            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500 text-right"><%= format_price(item.price, @order.currency) %></td>
            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500 text-right"><%= item.quantity %></td>
            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 font-medium text-right"><%= format_price(item.price * item.quantity, @order.currency) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <div class="mt-8">
    <%= link_to t('.back_to_orders'), account_orders_path, class: "text-blue-600 hover:underline" %>
  </div>
</div>
